# The .pc file is generated by the build system which uses the .pc.in file 
# in gtk-fortran/src and replaces the @.*@ strings by their values.

# Variables:
libname=@gtkfortranname@
prefix=@prefix@
exec_prefix=${prefix}
libdir=@prefix@/lib
includedir=@prefix@/include/${libname}

# Keywords:
Name: gtk-fortran
Description: A GTK / Fortran 2003 binding
URL: https://github.com/vmagnin/gtk-fortran
Version: @version@
Requires: @gtkname@
Cflags: @linkerflags@ -I${includedir}
Libs: -Xlinker -R${libdir} -l${libname}
